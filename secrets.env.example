# Active Corpus - Should be a folder name for a corpus that has been generated. If blanks corpus `default` will be used.
CORPUS_NAME=default

# Your email - Required by some API's to allow programatic downloads (Unpaywall).
USER_EMAIL=

# For storing API keys and access tokens for specific publishers.
WILEY_TDM_TOKEN=1
SPRINGER_API_KEY=
ELSEVIER_API_KEY=

# MinerU remote inference (optional, advanced). Required when transform_text(mineru_endpoint="vllm"):
# a vLLM server hosting the MinerU VLM model, plus the API key it was started with.
MINERU_VIRTUAL_VRAM_SIZE=
MINERU_VLLM_ENDPOINT=http://localhost:8000/v1
MINERU_API_KEY=